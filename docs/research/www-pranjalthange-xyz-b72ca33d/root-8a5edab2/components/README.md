# Component specs

Each builder agent writes its own `<Component>.spec.md` here as step 1 of its task, using the
global facts in `../BEHAVIORS.md` + `../PAGE_TOPOLOGY.md` and the app's own shipped source in
`../page-source.chunk.js` (the site is a Next.js app, so exact inline styles are recoverable
verbatim rather than estimated).
